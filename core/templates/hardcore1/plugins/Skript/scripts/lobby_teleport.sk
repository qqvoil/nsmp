# ============================================
# БЫСТРЫЙ ВОЗВРАТ В ЛОББИ (/hub, /lobby, /l)
# ============================================

command /hub:
    aliases: /lobby, /l
    trigger:
        send "&#aa00aa&l! &fПодключение к главному лобби..." to player
        connect player to "lobby"
